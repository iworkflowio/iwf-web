'use client';

import WorkflowSearchPage from './components/WorkflowSearchPage';

/**
 * Main Page Component
 * 
 * This is the entry point for the application.
 * It renders the WorkflowSearchPage component which contains all
 * the search functionality and UI for the workflow search interface.
 */
export default function Page() {
  return <WorkflowSearchPage />;
}